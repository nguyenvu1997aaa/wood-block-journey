namespace GameCore {
    namespace LiveUpdate {
        function Scene(key: sting, debug?: boolean): Function
        function SceneObjects(key: sting, debug?: boolean): Function
    }
}

namespace Phaser {
    namespace GameObjects {
        interface Container {
            hotContainer: boolean
        }
    }
}
